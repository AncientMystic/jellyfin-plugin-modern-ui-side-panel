# jprm build manifest (https://github.com/oddstr13/jellyfin-plugin-repository-manager)
# Single source of version truth. Build with: jprm build
# Produces per-TFM zips; publish one manifest per ABI (see README).
name: "Modern UI Side Panel"
guid: "9f7a2ad0-5e4e-4b2c-9c3a-1d2f6e7a8b9c1"
version: "1.0.0.0"
targetAbi: "12.0.0.0"
framework: "net10.0"
overview: "Keeps modern styling, restores persistent legacy side panel, hides top bar."
description: "Keeps the modern Jellyfin UI style, restores a persistent legacy left side panel, and hides the top bar header on desktop. Mobile keeps stock behaviour."
category: "General"
changelog: "Initial release: persistent sidebar + hidden top bar, modern glass styling, FileTransformation + disk-patch injection, legacy + v12 MUI support."
homepage: "https://github.com/jellyfin/jellyfin-plugin-modern-ui-side-panel"
owner: "Jellyfin Contributors"
license: "MIT"
